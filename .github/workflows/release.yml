name: Release

on:
  push:
    tags: ["v*"]

permissions:
  contents: read

env:
  QT_QPA_PLATFORM: offscreen

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"
      - name: Install Qt runtime libraries
        run: sudo apt-get update && sudo apt-get install -y libegl1 libgl1 libxkbcommon0
      - name: Install ExifTool
        run: sudo apt-get install -y libimage-exiftool-perl
      - name: Run checks
        run: |
          python -m pip install -e ".[dev]"
          ruff check .
          ruff format --check .
          mypy
          pytest -q
      - name: Tag must match the package version
        run: |
          version=$(python -c "import stampla_desktop; print(stampla_desktop.__version__)")
          test "v$version" = "$GITHUB_REF_NAME" || {
            echo "tag $GITHUB_REF_NAME does not match version $version" >&2
            exit 1
          }
      - name: Build distributions
        run: |
          python -m pip install build twine
          python -m build
          twine check dist/*
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: dist
          path: dist/

  publish:
    needs: build
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      # PyPI Trusted Publishing: configure this repository as a trusted
      # publisher on pypi.org (project: stampla-desktop, workflow:
      # release.yml, environment: pypi). No stored tokens.
      id-token: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dist
          path: dist/
      - uses: pypa/gh-action-pypi-publish@dc37677b2e1c63e2034f94d8a5b11f265b73ba33 # release/v1, v1.14.2

  github-release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: dist
          path: dist/
      - name: Extract this version's changelog section
        run: |
          awk -v version="${GITHUB_REF_NAME#v}" '
            $0 ~ "^## \\[" version "\\]" { found = 1; next }
            found && /^## \[/ { exit }
            found { print }
          ' CHANGELOG.md > release-notes.md
          test -s release-notes.md || {
            echo "no changelog section for $GITHUB_REF_NAME" >&2
            exit 1
          }
      - name: Create GitHub release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" dist/* \
            --repo "$GITHUB_REPOSITORY" \
            --title "$GITHUB_REF_NAME" \
            --notes-file release-notes.md

  macos-app:
    # Unsigned ad-hoc build: macOS blocks the first launch until the user
    # approves it under System Settings > Privacy & Security > Open Anyway.
    # Signing and notarization land when there is a Developer ID to sign with.
    needs: github-release
    runs-on: macos-15 # arm64
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: "3.13"
      - name: Build the app bundle
        run: |
          python -m pip install -e . pyinstaller
          python assets/make_icon.py
          pyinstaller --noconfirm Stampla.spec
      - name: App must start
        env:
          QT_QPA_PLATFORM: offscreen
        run: |
          dist/Stampla.app/Contents/MacOS/Stampla &
          sleep 8
          kill -0 $! || { echo "bundled app exited early" >&2; exit 1; }
          kill $!
      - name: Package the disk image
        run: |
          brew install create-dmg
          create-dmg \
            --volname Stampla \
            --window-size 540 380 \
            --icon-size 128 \
            --icon Stampla.app 140 180 \
            --app-drop-link 400 180 \
            "dist/Stampla-$GITHUB_REF_NAME-arm64.dmg" \
            dist/Stampla.app
      - name: Attach to the release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "$GITHUB_REF_NAME" \
            "dist/Stampla-$GITHUB_REF_NAME-arm64.dmg" \
            --repo "$GITHUB_REPOSITORY"
